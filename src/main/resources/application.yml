server:
  port: 8090
spring:
  datasource:
    url: jdbc:h2:mem:testdb;mode=MySQL
    username: root
    password: 1234
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true